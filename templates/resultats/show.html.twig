{% extends 'base.html.twig' %}

{% block title %}Resultats{% endblock %}

{% block body %}
    <h1>Resultats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultat.id }}</td>
            </tr>
            <tr>
                <th>Ip</th>
                <td>{{ resultat.ip }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resultats_index') }}">back to list</a>

    <a href="{{ path('resultats_edit', {'id': resultat.id}) }}">edit</a>

    {{ include('resultats/_delete_form.html.twig') }}
{% endblock %}
